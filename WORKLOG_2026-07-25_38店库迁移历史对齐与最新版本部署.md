# WORKLOG — 38 店库迁移历史对齐与最新版本部署

日期：2026-07-25

## 结论

已在用户明确授权后，完成 canonical 38 店数据库迁移历史对齐、缺失迁移执行、最新前后端部署和真实浏览器验收。

当前入口：

- 项目：http://127.0.0.1:8188
- 后端直连：http://127.0.0.1:8182
- 数据库：`ai_profit_os_real_qa`
- Git 提交：`2ac61777c309b61a6cc63205cd7631fa0a6796e4`

## 备份

执行任何历史写入前创建了新的全量备份：

- 文件：`/private/tmp/ai_profit_os_real_qa_pre_alignment_20260725_091843.sql.gz`
- 大小：约 1.1 GB
- SHA-256：`ffe76285e19170eca52787cf6e521d48629bfbb193e95344caa491650d1a275a`
- 校验：`gzip -t` 通过，末尾包含 `Dump completed on 2026-07-25 1:19:22`

## 历史对齐

全量审计发现不能只处理旧 V97/V98，还需要处理旧版合并的每日报损迁移：

- rank 90：旧组合脚本元数据对齐为当前 V93；
- rank 91：旧脚本名对齐为当前 V94；
- rank 95：V96 的空 checksum 对齐为当前 checksum；
- rank 96：旧 V97 商品叫货范围对齐为当前 V103；
- rank 97：旧 V98 门店档案对齐为当前 V104。

上述五条在同一事务内执行，带数据库身份、历史行数、精确旧记录、每日报损物理结构、仓库范围表和门店档案字段/索引/外键断言。事务结果：

- 前置断言：全部通过；
- 更新行数：5；
- 后置断言：全部通过；
- 决策：`COMMIT`。

未使用 `flyway repair`。

## 迁移执行

先使用全量备份恢复的一次性副本完整演练，再在 canonical 库执行。

一次性 QA runner 开启 `spring.flyway.out-of-order=true`，实际执行：

- V91：每日报损计价单位；
- V92：每日报损计价快照；
- V97：督导全部现有门店范围；
- V98：利润表结构修复及真实成本回填；
- V99：督导员工档案读取权限；
- V100：督导报损权限修复；
- V101：督导与财务员工档案维护权限；
- V102：培训视频分类乱码修复。

最终 Flyway 状态：

- 历史行数：105；
- 最大 installed rank：105；
- 失败记录：0；
- 当前版本：V104；
- 严格模式 `out-of-order=false` 再次启动：验证 105 条迁移，无待执行迁移。

## 数据核验

迁移后：

- 门店：38 家“营业中”+ 5 家“已闭店”；
- 账号：45；
- 知识库资料：49 条；
- 督导门店范围：43 家；
- 督导 permission version：21；
- 财务、督导均具有 `employee.read` 和 `employee.manage`；
- 目标培训分类乱码剩余：0。

## 部署

最新镜像：

- 后端：`ai-profit-os-dev-backend:head-2ac6177`
- 前端：`ai-profit-os-dev-frontend:head-2ac6177`

当前容器：

- `ai-profit-os-real-backend-head-2ac6177-20260725`
- `ai-profit-os-real-frontend-head-2ac6177-20260725`
- `ai-profit-os-real-gateway-1`
- `ai-profit-os-qa-38-mysql`

网关 `backend-real` 和 `frontend-real` 别名只指向最新容器。旧 `real-v98` 后端和 `real-v94` 前端已停止但未删除。最新前后端 restart policy 均为 `unless-stopped`。

## 验收

接口验证：

- `/api/health`：200；
- BOSS `/api/auth/me`：同时包含 `knowledge_base.search`、`knowledge_base.manage`；
- `/api/knowledge-base/documents/available`：49 条，49 条均有发布时间；
- `/api/stores?knowledgeBaseScope=true`：43 家。

浏览器验证：

- BOSS 登录正常；
- 显示“我可查看的资料”；
- 显示“上传并发布”和“仅保存草稿”；
- 指定门店展示名称、编号、区域和状态；
- 显示 43 个复选项；
- 选择 `rg1`、`rg10` 后准确显示“已选择 2 家门店”。

本次未新增或修改任何 Flyway 源文件。
