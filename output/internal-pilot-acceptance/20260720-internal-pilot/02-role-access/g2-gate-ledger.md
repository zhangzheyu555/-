# G2 六角色访问、范围与桌面端台账

执行批次：`E2E-20260720`
执行时间：2026-07-20（本机隔离 QA）
范围：桌面 Web；不包含移动端。

## 当前结论

`G2 PASS`。六个正式角色均完成登录、默认工作台、菜单、直接路由拒绝、数据范围、会话失效、操作审计和桌面分辨率验证。已修复两项阻断缺陷并完成回归。

## 角色验证

| 角色 | 默认工作台与允许读取 | UI 菜单/路由 | 禁止项（前端/后端） | 范围与会话 | 结论 |
| --- | --- | --- | --- | --- | --- |
| BOSS | `/boss`；老板、财务、仓库、门店、督导、账号与日志核心读取均 200 | 默认进入老板工作台；全局门店选择器、授权菜单可见 | 不适用（仍受单租户边界） | 全域；退出后旧令牌访问老板工作台为 401 | PASS |
| FINANCE | `/finance`；财务、日报损只读、门店读取均 200 | 仅财务相关菜单；财务工作台正常渲染 | `/users`、巡检、仓库概览均 403；直达 `/admin/users` 跳转中文无权限页 | 财务/工资/门店范围为 A、B 两店；退出后旧令牌 401 | PASS |
| WAREHOUSE | `/warehouse`；仓库、物料、仓库概览均 200 | 仅仓库与辅助菜单；总仓、分仓列表正常渲染 | 财务、账号、巡检、日报损均 403；直达 `/admin/finance` 跳转中文无权限页 | 仓库范围仅 `1,2`；退出后旧令牌 401 | PASS |
| STORE_MANAGER | `/store`；本店工作台、门店 A 报损、门店列表均 200 | 本店经营、报损、叫货/库存、整改等菜单正常；无财务录入/账号权限入口 | 请求门店 B 报损为 403；财务、账号为 403；物料维护写探针为 403；直达 `/admin/users` 跳转中文无权限页 | 门店列表仅 A；仓库概览隐藏采购/流水数据；退出后旧令牌 401 | PASS |
| SUPERVISOR | `/operations`；巡检、稽核标准、日报损、平台读取均 200 | 督导巡店、整改复核、培训、平台配置入口正常 | 财务、总仓、账号、工资均 403；直达 `/admin/finance` 跳转中文无权限页 | 授权门店 A、B；退出后旧令牌 401 | PASS |
| EMPLOYEE | `/employee`；本人工作台、资料、员工助手状态均 200 | 仅学习、本人资料与员工助手菜单；本人门店 A 信息可见 | 员工列表、财务、仓库、账号、巡检均 403；直达 `/admin/users` 跳转中文无权限页 | 考试范围为 SELF；退出后旧令牌 401 | PASS |

## 公共桌面与审计证据

- 未登录访问 `http://127.0.0.1:18088/admin/` 直达 `/admin/login`，不会再被错误送往无权限页。
- 每个角色在 `1366×768`、`1440×900`、`1920×1080` 三种桌面视口下均满足 `scrollWidth === clientWidth`；主内容区正常伸缩，无整页横向滚动。
- `operation_log` 已记录每个角色的退出会话；FINANCE、WAREHOUSE、STORE_MANAGER、SUPERVISOR、EMPLOYEE 的拒绝接口同时产生 `permission_denied` 审计；EMPLOYEE 的允许读取产生 `employee_assistant.health` 审计；BOSS 的受控账号密码恢复产生账号维护审计。测试中使用的临时 UI 口令已恢复，最终以原 QA 口令登录 BOSS 返回 200。
- 历史 `OPS/OPERATIONS` 账号不再存在于运行 QA 库；G1 独立 V71→V72 升级断言已证明其迁移为 `SUPERVISOR`、旧令牌删除、历史数据可追溯。

## 本关修复与回归

1. **日报损老板查询 500（P1）**：BOSS 被通用角色判断误当作店长，触发缺少门店绑定的只读事务写审计。改为仅在角色精确为 `STORE_MANAGER` 时应用单店绑定；新增 `DailyLossServiceTest` 回归。重新构建后 BOSS `/api/daily-loss/reports` 为 200。
2. **根入口登录后错误无权限（P1）**：匿名访问 `/admin/` 先被首页重定向到 `/no-permission`，登录页错误沿用该地址回跳。路由在未登录时直接进入登录页；BOSS 登录后进入 `/admin/boss` 并完整渲染工作台。

## 放行到 G3 的条件

G2 已满足。下一步仅可进入 G3，并严格按 45 项功能矩阵执行；每个功能必须完成正常、错误、权限、MySQL 与操作日志、桌面页面验收，任何失败立即回到该项首例。
