# G4 FLOW-04 真实隔离 QA 验收

执行时间：`2026-07-21`
范围：`FINANCE → BOSS → STORE_MANAGER` 的合成月度经营录入、汇总、范围读取与 CSV 导出。

## 结论

`PASS`。财务在已授权门店保存一条合成月度经营记录；BOSS 全局汇总、店长本店只读与财务范围内 CSV 导出一致。写入、导出和拒绝边界均在专用 loopback QA MySQL 中验证。

## 闭环证据

| 项目 | 结果 |
| --- | --- |
| 数据与隔离 | 仅访问 `127.0.0.1:18181` QA 候选服务及 `127.0.0.1:13307` 专用 QA MySQL；使用门店 A 和 `2026-07` 的合成记录，未触及生产、局域网或真实数据。 |
| FINANCE 保存 | `200`；保存一条门店 A 合成记录。SQL 金额：销售 `1000.00`、退款 `100.00`、折扣 `50.00`、原料 `300.00`、包材 `50.00`、损耗 `10.00`、其他成本 `40.00`、租金 `100.00`、人工 `200.00`。 |
| BOSS 全局汇总 | `200`；同品牌范围内一条目标门店记录，服务端 BigDecimal 汇总：收入 `850.00`、成本 `400.00`、费用 `380.00`、净利 `70.00`。 |
| STORE_MANAGER 本店只读 | 本店详情 `200` 且净利 `70.00`；伪造门店 B 为 `403`；对相同结构的保存请求为 `403`。 |
| 匿名边界 | 匿名查询为 `401`。 |
| FINANCE CSV | `200`、`text/plain`，只有一条数据行，范围参数为门店 A；下载不改写经营记录。 |
| 审计 | 保存审计一条、利润排行导出审计一条，均为 `tenant_id=1`；导出审计同时绑定门店 A 与 `2026-07`。 |

临时 BOSS、FINANCE、STORE_MANAGER 会话均正常登出；响应与 CSV 只保存在用户私有临时目录后移至本机废纸篓。未执行移动端、WebKit 或 G5 的文件故障专项测试。

FLOW-04 通过不代表 G4 通过；FLOW-05 至 FLOW-07 仍需逐项完成。
