# G3 45 项模块验收台账

执行批次：`E2E-20260720`
范围：桌面 Web；不包含移动端。

当前状态：`IN PROGRESS`。WS-01 至 WS-07、FIN-01 至 FIN-06 及 STORE-01 已通过；本次在 STORE-01 后停止，未进入 STORE-02。

| 用例 | 正常路径 | 错误/边界路径 | 权限/范围 | 数据与日志 | 桌面页面 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| WS-01 老板工作台 | BOSS 登录后默认进入 `/admin/boss`；`/api/boss/todo-dashboard` 返回结构化 MySQL 数据，`todayFocus=7`、`needsBossAction=0`；空待办状态显示为中文提示 | `limit=-1` 被服务端安全归一为默认分页并保持 200，不产生异常查询或写入 | FINANCE 调用老板工作台 API 为 403；前端角色守卫拒绝无权限访问 | `business_todo` 的 PENDING 记录保留为 2 条；工作台读取未写业务数据；会话退出写入 `operation_log` | BOSS 页面完整渲染；1366×768、1440×900、1920×1080 无横向滚动 | PASS |
| WS-02 财务工作台 | FINANCE 默认进入 `/admin/finance`；`/api/finance/workbench` 返回结构化数据；两家授权门店均在页面范围选择器中可见 | `month=not-a-month` 返回 400、`BAD_MONTH` 与中文“月份格式必须为 YYYY-MM” | 店长访问财务工作台为 403；财务请求伪造门店为 403；直达 `/admin/warehouse` 跳转中文无权限页 | `profit_entry` 当前为 0，和页面空基线一致；跨范围拒绝写入 `operation_log(permission_denied, STORE, foreign-store-e2e)`；会话退出有审计 | 财务工作台、模块入口与两门店范围正常渲染 | PASS |
| WS-03 仓库工作台 | WAREHOUSE 默认进入 `/admin/warehouse`；可见 2 个启用仓库；指定总仓概览返回 2 个物料 | `warehouseId=999999` 返回 404，未产生写入 | FINANCE 访问仓库概览为 403；G2 已验证财务直达仓库页面跳转中文无权限页 | `warehouse_facility` 启用记录为 2，和 API/页面仓库表一致；会话退出有审计 | 总仓、分仓、供货拓扑和仓库表完整渲染 | PASS |
| WS-04 店长工作台 | STORE_MANAGER A 默认进入 `/admin/store`；本店工作台、门店资料、报销入口、工资核对、日报损、库存与整改入口正常 | 当前月无经营数据时展示金额 0、暂无利润记录和中文空态，不产生伪造数据 | STORE_MANAGER B 请求门店 A 报损为 403；FINANCE 访问店长工作台为 403；店长直达数据录入被导向本店只读利润表 | `store_branch` 中 A、B 两店均存在；店长 B 跨店拒绝写入 `operation_log(permission_denied, STORE, e2e-20260720-store-a)`；会话退出有审计 | 页面明确展示门店 A 名称、编号、品牌、区域、负责人、营业状态及本店功能菜单 | PASS |
| WS-05 督导巡店工作台 | SUPERVISOR 可读取巡检记录、稽核标准、整改复核及 2026-07 日报损；返回分别为 0 条、4 个标准版本、0 条和 62 条报损记录 | 非法开始日期返回 400、`BAD_DATE` 与中文“开始日期必须使用 YYYY-MM-DD 格式”；开始日期晚于结束日期返回 400、`BAD_DATE_RANGE` 与中文“开始日期不能晚于结束日期”；已补单测覆盖并通过 `InspectionRecordServiceTest`，后端打包通过 | 伪造门店 `foreign-store-e2e` 返回 403；WAREHOUSE 访问巡检 API 返回 403；SUPERVISOR 直达 `/admin/finance` 跳转 `/admin/no-permission?from=/finance` 并显示中文无权限提示 | `inspection_record=0`、`inspection_standard_version=4`、`inspection_rectification=0` 与 API、页面空基线一致；跨门店拒绝已写入 `operation_log(permission_denied, STORE, foreign-store-e2e)`；受控临时密码已恢复，原 QA 密码登录 200、角色为 SUPERVISOR | 督导巡店页完整渲染巡检记录、发起巡检、稽核标准三页签；两家授权门店可选；展示标准版本 `2025.11.06-R1`、红线 21、黄线 9、评分条款 105、满分 200、合格线 180 | PASS |
| WS-06 督导工作台 | SUPERVISOR 的角色待办、盘存、考试中心和企迈接入状态接口均为 200；待办、盘存为空基线，培训资料 9 条、考试卷 3 份、企迈配置 1 条 | 非法待办状态返回 400、`BAD_STATUS` 和中文“待办状态仅支持风险、待处理、提醒或已完成”；后端已补精确消息回归 | 跨授权门店返回 403；WAREHOUSE 读取督导待办为 403 且中文提示；FINANCE 访问考试中心为 403；匿名访问待办为 401；财务读取盘存属于合法 `inventory.read`，未误判越权 | 跨门店待办查询已改走统一授权入口；单次拒绝使 `operation_log(permission_denied, STORE, foreign-store-e2e)` 精确增加 1 条；财务考试拒绝也有权限日志；读取未改写业务表 | SUPERVISOR 默认进入 `/admin/operations`；督导巡店、整改复核、培训考试、平台配置 4 个入口完整；两家授权门店可选；1280 宽无横向溢出；直达财务跳中文无权限页；干净会话控制台错误 0；临时密码已恢复并验证原密码登录 | PASS |
| WS-07 员工工作台 | EMPLOYEE A 的 `/api/employee/workbench` 与 `/api/employee/profile` 均为 200，仅返回本人、门店 A 和已关联档案；未配置员工助手显示 `UNCONFIGURED` 且不可打开；员工 B 仅绑定门店 B | 员工账号未绑定门店、门店不存在的中文业务提示已有单测；员工助手未配置不再显示为可用，已新增单测 | 匿名工作台为 401；员工 A 读取员工 B 档案、读取员工列表，以及 SUPERVISOR 访问员工工作台均为 403，且中文拒绝信息正确 | 工作台读取员工助手健康状态写入 `employee_assistant.health`；三次拒绝均写入 `permission_denied`；档案关联优先按 `authUserId`，不会因显示姓名不同而误判未关联 | EMPLOYEE A 登录后仅见员工工作台、培训考试、我的资料/工资、员工服务助手；1280 宽无横向滚动；页面显示门店 A 和禁用的“打开助手”；资料页显示已关联档案、正确员工编号、岗位与门店；登录后无新增控制台错误 | PASS |
| FIN-01 利润概览 | 通过 FINANCE 正式接口创建并保留 `E2E-20260720 FIN01 fixture`：门店 A（2026-06、2026-07）和门店 B（2026-07）。财务和老板查询 2026-07 均返回 2 条、营业收入 21850.00、成本 8600.00、净利 3350.00、净利率 0.1533；店长 A 仅返回本店 9850.00 营业收入、1950.00 净利；2026-06 趋势为 1650.00 净利 | `month=not-a-month` 返回 400、`BAD_MONTH` 和中文“月份格式必须为 YYYY-MM” | EMPLOYEE 查询为 403；匿名为 401；店长 A 伪造门店 B 为 403，中文说明“当前账号只能访问绑定门店的数据” | QA MySQL 的 3 条夹具与接口聚合逐项一致；一次跨店拒绝使 `operation_log(permission_denied, STORE, e2e-20260720-store-b)` 对店长 A 精确增加 1 条 | 财务桌面利润概览显示合并收入 ¥21,850、净利 ¥3,350、2 条记录、两店排名和 6/7 月趋势；1280 宽无横向滚动 | PASS |
| FIN-02 利润表 | FINANCE 查询 2026-07 汇总表返回门店 A、B 两条记录；页面显示收入 ¥21,850、成本 ¥8,600、费用 ¥9,900、净利 ¥3,350、净利率 15.3%。分页接口 `size=1` 正确返回第 1/2 页且行不重复；`page=0,size=999` 安全归一为第 1 页、每页 100 条；最大整型页码返回空页而不异常 | 非法月份返回 400、`BAD_MONTH`；极端页码不发生整数溢出或 500 | 店长 A 请求门店 B 为 403；EMPLOYEE 为 403；匿名为 401 | QA MySQL 保持 3 条利润夹具；跨店拒绝使店长 A 对目标门店 B 的 `permission_denied` 审计精确增加 1 条 | 1280 宽下汇总模式表格、品牌/月筛选与合计行正确渲染，横向宽度等于视口、控制台错误为 0。当前仅 2 家夹具，前端“超过 20 家门店”翻页控件不出现；其页面显示条件与接口分页均已代码构建及接口验证 | PASS |
| FIN-03 经营数据录入与导入 | FINANCE 为门店 A、2025-12 手工录入后，收入 850、成本 235、毛利 615、毛利率 72.35%、费用 305、净利 310、净利率 36.47%，详情、概览与 MySQL 一致；同月连续保存和接口重试后始终仅 1 条且最终值一致。受控 `.xlsx` 单门店预览、确认导入、重复确认幂等、冲突覆盖和覆盖后汇总均通过 | 负金额、空门店、非法月份、品牌门店不匹配、超 12 位金额均为中文 400；失败后无半条记录。多门店/未知门店/重复门店月份 Excel 预览为 `FAILED`，逐行指出跨门店、未匹配门店或重复月度汇总，数据库零写入；未确认覆盖为 409 中文提示 | FINANCE 与 BOSS 均可执行既有授权的手工录入/导入预览；SUPERVISOR、STORE_MANAGER、EMPLOYEE 的手工录入和导入预览均为 403，跨店参数未绕过；匿名写入为 401 | 每次成功和拒绝均在 `operation_log` 记录操作者、动作、目标、门店、月份、中文结果；拒绝日志以独立事务落库。FIN-03 临时记录（2025-10/11/12）已由 BOSS 受控删除，QA 基线 2026-07 仍为 2 条 | 财务录入页、导入抽屉、利润概览、利润表在 1280px 均 `scrollWidth=clientWidth=1280`；导入确认按钮在未选择文件时禁用；干净页面控制台错误 0。FIN-01 汇总仍为 ¥21,850 / ¥3,350；FIN-02 `size=1` 的第 1/2 页分别为门店 A/B | PASS |
| FIN-04 报销录入与审批 | 店长仅本店完成草稿、提交、补资料、重提；财务与老板按现有范围审批；主凭证及补充附件上传、预览、下载、删除完整可用；审核完成后单据不可再改 | 空值、负数、三位小数、非法日期/月、过长文本、缺门店、品牌门店不匹配均为中文 400 且无残留；重复/并发提交与审批仅产生一次状态迁移；已完成单据不可再审批；通用待办入口不能绕过专用审批 | FINANCE/BOSS 在授权范围操作；SUPERVISOR、WAREHOUSE、EMPLOYEE 审批为 403；店长跨店/跨品牌为 403；匿名为 401；SUPERVISOR 页面直达报销栏跳中文无权限页 | 成功、拒绝、附件操作和通用入口拒绝均有 `operation_log`，含操作者、目标、门店、月份、结果/原因；待补资料无活跃财务待办；5 个临时单据、附件、待办/动作与审计已精确清理，FIN-01/02/03 基线复核不变 | 店长提交、财务通过、督导无权限页实测；报销、利润概览、利润表、数据录入均在 1280px 无横向溢出、控制台错误 0；后端全量测试、打包、前端构建和桌面 Chromium 4/4 报销回归通过 | PASS |
| FIN-05 工资与本店工资核对 | FINANCE/BOSS 在授权范围保存工资、精确舍入、生成工资、提交、审核、驳回、重提、发放与锁定均通过；应发与实发、明细、快照和列表/详情/数据库一致 | 空值、负数、超范围金额/工时、非法月份、跨门店员工、工资明细与应发不一致均为中文 400；重复周期为 409；连续/并发提交、审核、发放仅一方成功，另一方为版本或状态冲突；失败零写入 | BOSS、FINANCE 按当前代码具备工资读写/审核范围；STORE_MANAGER 仅本店查看且金额脱敏、无写入；SUPERVISOR、WAREHOUSE、EMPLOYEE 直调工资接口均为 403；匿名为 401；店长跨店为 403 | 创建、提交、驳回、审核、发放、锁定和拒绝均已核验 `operation_log` 的操作者、目标工资、门店、周期、前后状态与结果；拒绝审计独立保留。4 条工资、2 条明细、规则/档案/考勤、测试 BOSS、待办、审计和会话均按精确 ID 清理，残留为 0 | 财务工资页、店长本店工资页、员工我的资料/工资页均在 1280px 无横向溢出、控制台错误 0；店长只见本店脱敏数据，员工只见本人；桌面 Chromium 健康与六角色路由 19/19 通过；FIN-01 至 FIN-04 基线复核不变 | PASS |
| FIN-06 经营数据导出 | BOSS 与 FINANCE 受控 QA 账号均可导出利润、报销、工资 CSV；指定门店、两门店和全部授权范围的行数均与 MySQL 一致，重复下载不新增业务数据；工资 CSV 使用实发工资，公式前缀 `= + - @` 均以单引号转义 | 非法月份为中文 400、跨范围门店为 403；重复下载保持内容和业务数据一致 | BOSS/FINANCE 成功；SUPERVISOR、STORE_MANAGER、WAREHOUSE、EMPLOYEE 均为 403，匿名为 401；店长即使临时误配 `finance.export` 仍被前端路由和后端角色上限拒绝 | 成功、非法月份和五次具名拒绝均核验审计的操作者、对象、门店、月份、范围/结果；临时工资、权限覆盖、会话和本轮审计精确清理，QA 基线恢复为利润 2 条、报销 0、工资 0 | BOSS 导出页在 1280px `scrollWidth=clientWidth=1280`，三项下载可用且控制台错误 0；桌面 Chromium 导出回归 4/4 通过 | PASS |
| STORE-01 每日报损与按月 Excel 数据导出（无照片包） | 隔离 H2 验证 BOSS/FINANCE 的 `.xlsx` 含“每日汇总”“报损明细”、未报日期、金额/数量数值单元格、筛选、冻结表头与合成数据汇总一致；附件存在时工作簿无图片、绘图、附件名/URL 或 ZIP 内容 | 非法/失败导出提示、月份参数、下载响应头、重复下载不写业务表均已定向/Chromium 回归；导出仅新增审计 | BOSS/FINANCE 成功；STORE_MANAGER、SUPERVISOR、WAREHOUSE、EMPLOYEE 无菜单/路由入口且接口 403；店长、督导注入遗留读/导出权限仍被守卫和后端角色上限拒绝；匿名 401；财务跨店 403、跨租户查询只使用认证租户 | `daily_loss_export` 审计只记录月份、门店范围和行数；拒绝按既有 `permission_denied` 规则记录。两次导出业务数据不变、仅有两条成功导出审计；全程 H2 合成数据，运行结束后临时数据、误配、会话、审计均释放，QA 基线未改写 | 相关 Chromium 两套回归通过；BOSS/FINANCE 报损页与角色菜单守卫在 1280px 无横向溢出，无“导出照片包”入口 | PASS |

下一动作：STORE-01 已通过；等待授权后再开始 STORE-02。本次不进入下一门禁。

## 2026-07-21 最终执行补充（覆盖上述旧的“下一动作”）

当前状态：`DEFERRED（未闭合）`。45 项中 44 项已通过；`QMAI-04` 因尚未取得权威配方目录，按业务决定暂缓，**不得标记为 PASS**。

| 模块 | 结论 | 证据 |
| --- | --- | --- |
| WS-01～WS-07 | PASS（7/7） | 本台账既有记录 |
| FIN-01～FIN-06 | PASS（6/6） | 本台账既有记录及 [FIN-06](fin-06-export-acceptance.md) |
| STORE-01～STORE-03 | PASS（3/3） | [STORE-01](STORE-01-acceptance-report.md)；STORE-02/03 隔离复验记录 |
| WH-01～WH-10 | PASS（10/10） | [WH-01～03](wh-01-to-wh-03-isolated-acceptance.md)；WH-04～10 定向复验记录 |
| INS-01～INS-05 | PASS（5/5） | [INS 重验](ins-01-to-ins-05-retest.md) |
| EMP-01～EMP-04 | PASS（4/4） | [EMP 最终重验](emp-01-to-emp-04-final-retest.md) |
| QMAI-01～QMAI-03 | PASS（3/3） | [QMAI 最终重验](qmai-01-04-final-retest.md) |
| QMAI-04 | DEFERRED | [QMAI 最终重验](qmai-01-04-final-retest.md)；[IP-P2-017](../defects-and-waivers.md) |
| AI-01～AI-02 | PASS（2/2） | [AI 验收](ai-01-to-ai-02-acceptance.md) |
| GOV-01～GOV-03 | PASS（3/3） | [GOV 验收](gov-01-to-gov-03-acceptance.md) |
| FLOW-01 | PASS（1/1） | [FLOW 验收](flow-01-acceptance.md) |

本轮最终本地验证：`mvn -q test` 为 **855/855、0 failure、0 error、0 skipped**；`mvn -q -DskipTests package`、`vue-tsc -b`、`vite build` 均退出码 0。新增的本地验收只使用 H2 合成数据、浏览器 Mock 与回环 Vite；未在本轮连接 QA MySQL、Docker、局域网或生产。移动端不在本轮范围。

QMAI-04 后续补齐条件仍为：提供经审核的租户/品牌权威配方目录，或批准可审计的 BOSS/督导维护/导入契约；随后重新执行 QMAI-04。用户已于 2026-07-21 明确授权该项暂缓并进入 G4；该授权仅解除 G4 前置，不将本项改写为 PASS，也不满足最终 45/45 签署条件。
