# FIN-06 经营数据导出验收报告

执行批次：`E2E-20260720`
结论：`PASS`

## 证据摘要

- 范围：桌面 Web、隔离 QA MySQL、受控 E2E 账号；未使用真实业务数据。
- 接口：BOSS 与 FINANCE 的利润、报销、工资 CSV 均为 200；指定门店和全部授权门店的 CSV 行数与 QA MySQL 一致。重复下载只新增独立下载审计，不写入业务表。
- 安全：非法月份为中文 400，财务伪造门店为 403；SUPERVISOR、STORE_MANAGER、WAREHOUSE、EMPLOYEE 为 403，匿名为 401。店长临时误配 `finance.export` 后仍为 403。
- CSV：工资列“实发工资”使用 `net_pay`；以 `= + - @` 开头的员工字段在 CSV 中均被单引号转义，未触发公式。
- 审计：成功、非法月份和具名权限拒绝均记录操作者、导出对象、门店、月份、范围说明与结果。
- 页面：BOSS 导出页在 1280px 下 `scrollWidth=clientWidth=1280`；三项下载按钮正确、浏览器控制台错误为 0。
- 回归：桌面 Chromium 导出回归 4/4 通过；后端全量测试、后端打包、前端类型检查与生产构建通过。

## 清理证明

临时工资记录 `fin06-export-salary-formula`、店长个人权限覆盖、六角色会话及本轮导出审计均已精确清理。复核 QA 基线：2026-07 利润记录 2 条，报销 0 条，工资 0 条；临时记录、临时权限和 E2E 会话均为 0。

## 下一门禁

进入 STORE-01：每日报损及按月 Excel 数据导出（不导出照片包）。
