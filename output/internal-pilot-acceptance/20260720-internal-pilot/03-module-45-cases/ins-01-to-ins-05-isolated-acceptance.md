# INS-01～INS-05 巡检隔离验收报告

执行批次：`INS-01-05-H2-20260721`
结论：`BLOCKED`

## 范围与隔离

- 仅审计 INS-01 发起巡检、INS-02 巡检记录/问题、INS-03 稽核标准、INS-04 门店整改、INS-05 督导复核的桌面 Web 与对应后端 H2 测试。
- 后端定向验证使用测试生命周期内的 H2 内存库与合成数据；整改工作流迁移测试验证空库迁移链，不连接 QA MySQL、Docker 数据库、局域网或生产环境。
- 未修改业务代码、后端逻辑、数据库迁移或 `g3-gate-ledger.md`；未提交代码。

## 已通过证据

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| 巡检定向测试 | PASS | 35/35，0 failure/error：记录创建/读取、输入校验、门店范围、标准诊断、整改提交、匿名/角色/跨店拒绝、迁移工作流 |
| H2 迁移 | PASS | `InspectionRectificationWorkflowMigrationTest` 在空 H2 验证整改工作流迁移；Flyway 校验 62 条迁移 |
| 后端全量测试 | PASS | 833/833，0 failure/error |
| 后端打包 | PASS | `mvn -q -DskipTests package`，JAR 已生成 |
| 清理 | PASS | 内存 H2 与合成数据随测试进程释放；本地 Vite `18173` 已关闭；未写 QA 基线 |

## 已验证的规则

- INS-01/02：督导可在同租户范围创建、读取巡检记录；店长仅见本店，越店读取/写入被拒绝；日期、门店、分数和日期范围的非法输入返回业务错误；记录更新/删除写审计。
- INS-03：仅读取当前租户启用的标准，缺失或不合格的 105 条/200 分标准会给出可操作的中文诊断并阻止保存。
- INS-04：匿名提交为 401；错误角色和跨店店长提交为 403；提交只写独立整改工作流及操作审计，不改写历史巡检得分或快照。
- INS-05：店长在复核入口被角色检查提前拒绝；代码中的复核更新以 `PENDING_REVIEW` 为条件，设计上防止覆盖已复核状态。

## 阻塞证据

### 1. 桌面 Chromium 回归失败

执行现有巡检 Chromium 集（15 项）后为 **11 通过、4 失败**，因此不满足本轮“任一失败即 BLOCKED”的门禁。

1. `14-inspection-standard-export`：标准摘要 `105条（物料40 / 卫生47 / 服务18）· 200分 · 合格线180分` 未找到。
2. `14-inspection-standard-export`：`2026.07-R1` 使用严格定位时命中两个元素。
3. `18-inspection-detection-confirm`：拍照/选图按钮高度为 36px，低于断言的 44px。
4. `18-inspection-detection-confirm`：受认证附件 `502` 未携带预期的 Authorization 头。

说明：该现有 Chromium 集混有一条移动端命名用例；其结果未作为本轮桌面验收证据，也未进行任何移动端人工或浏览器验收。

### 2. INS-04/INS-05 关键覆盖不足

- 现有整改服务测试未覆盖“督导/老板成功复核”后的审计、复核人/时间/结果持久化。
- 未覆盖同一整改的第二次复核、并发双复核条件更新与不可覆盖断言。
- 未发现门店整改页和整改复核页的独立桌面 Chromium 回归，因此无法以页面证据确认整改提交、复核通过/驳回、中文错误态及 1280px 无横向溢出。
- 跨租户隔离已在巡检记录 H2 范围测试中存在，但整改/复核工作流没有对应的 H2 跨租户正反例；不能将记录层覆盖外推为整改复核已验收。

## 未执行

- `vue-tsc -b` 与生产构建：**NOT RUN**。按照“任一失败即 BLOCKED”，在 Chromium 失败后停止后续依赖验证。
- 不进入任何后续门禁或业务项。

## 最小解锁条件

1. 修复上述 4 个 Chromium 失败，新增/更新仅桌面 1280px 巡检回归并全部通过。
2. 补充独立 H2 合成测试：店长提交整改 → 督导/老板通过或驳回 → 审计、复核人/时间/结果断言；重复及并发双复核只能成功一次；跨店、跨租户、匿名和 SUPERVISOR 权限上限均需明确断言。
3. 补充门店整改与督导复核的桌面 Chromium 用例，验证 URL、队列状态、错误态、控制台错误和无横向溢出。
4. 仅在上述项通过后重新执行前端类型检查、生产构建和本验收门禁。
