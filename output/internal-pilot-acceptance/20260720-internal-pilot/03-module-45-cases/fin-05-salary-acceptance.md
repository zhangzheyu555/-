# FIN-05 工资与本店工资核对验收证据

执行批次：`E2E-20260720`
范围：隔离 QA MySQL、受控 E2E 门店与账号、桌面 Web（1280px）；不包含移动端；未读取、导出或修改真实业务数据。

## 结论

`FIN-05 PASS`。工资数据、状态流转、本店工资核对、六角色权限/数据范围、审计、桌面页面、前序财务回归和临时数据清理均通过。

本轮遵循现有实现，不虚构审批或确认规则：工资实际状态为 `DRAFT -> SUBMITTED -> APPROVED -> PAID -> LOCKED`，以及 `SUBMITTED -> REJECTED ->（修改后）SUBMITTED`；锁定也允许从已审核状态直接进入。当前没有独立的“本店工资确认”接口、状态或待办，店长功能是受限的本店只读核对，故未擅自新增确认动作。

## 1. 工资数据、计算与幂等

| 项目 | 验收证据 | 结论 |
| --- | --- | --- |
| 手工保存与计算 | 财务为门店 A、员工 A、`2026-10` 保存受控完整工资明细，按现有页面字段公式得到应发/实发 `1127.00`；刷新详情、工资列表和 MySQL 的 `gross/net_pay` 一致。 | PASS |
| 精度与错误校验 | `.005` 金额按 HALF_UP 为 `.01`；负金额、负/超范围工时、空员工、空门店、非法月份、超范围金额、工资明细与应发不一致均为明确中文 400；员工 B 伪造门店 A 被拒绝为 `EMPLOYEE_STORE_MISMATCH`。失败后数据库无新增工资。 | PASS |
| 重复与并发 | 同员工同周期重复保存返回中文 409 `SALARY_ALREADY_EXISTS`，顺序保存和接口重试不产生第二条；提交、审核、发放并发只有一个请求成功，另一个为版本或状态 409，最终工资状态与金额一致。 | PASS |
| 自动生成 | 受控工资规则、员工档案和考勤生成 `2026-11` 工资，结果为 1 条生成、2 个工资明细、应发/实发 `2520.00`；重复生成结果为 0 新增、3 个跳过。规则、快照和明细均落库。 | PASS |

接口覆盖：`POST/PUT /api/salaries`、`GET /api/salaries`、`GET /api/salaries/{id}`、`POST /api/salaries/generate-report`，以及提交、审核、驳回、发放、锁定五个状态接口。数据库逐项核验 `salary_record`、`salary_record_item`、`salary_policy`、`employee_salary_profile`、`employee_month_attendance` 和 `operation_log`。

## 2. 状态、审批和审计

- 工资 A：保存、提交、审核、发放、锁定后为 `LOCKED`、版本 `5`；锁定后更新或再次状态迁移均为中文 409，数据不变。
- 工资 B/C：覆盖 `提交 -> 驳回（含中文原因）-> 修改 -> 重提 -> 审核通过`；复测后通过状态的 `review_note` 为 `NULL`，不会残留旧驳回原因。
- 每个保存、提交、驳回、审核、发放、锁定动作均在当时核验 `operation_log`：操作者、工资目标、门店、`2026-10` 周期、前后状态和结果一致。
- 本轮修复后，店长跨店及督导、仓库、员工的拒绝审计为 `target_type=salary_record`，记录目标工资或权限、门店 A 与工资周期；拒绝不随业务事务回滚。

## 3. 本店工资核对与角色边界

| 角色 | 页面/API 验收结果 |
| --- | --- |
| BOSS | 可按当前全局权限查看两店工资并参与审核/发放路径；跨店工资状态变更按范围和版本控制。 |
| FINANCE | 可在两家授权门店录入、生成、提交、审核、发放及锁定；财务工资页的记录、金额和审计均一致。 |
| STORE_MANAGER A | `/admin/store/salary` 仅显示门店 A 的工资清单；接口可读但工资金额字段脱敏，写入/审核为 403；伪造门店 B 为 403。 |
| SUPERVISOR | 已接管运营职责，但不取得工资读写、审核或发放权限；直接接口均为 403。 |
| WAREHOUSE | 工资读写均为 403。 |
| EMPLOYEE A | 不开放通用工资管理；个人资料接口仅显示本人、门店 A 和本人工资，未暴露其他员工记录或入口。 |
| 匿名 | 工资读取和写入均为 401。 |

## 4. 桌面页面与前序回归

- 财务工资页、店长本店工资页、员工“我的资料/工资”页均以 `1280px` 桌面视口实际检查：`scrollWidth=clientWidth=1280`，无横向溢出、加载/空态/禁用状态可用，控制台错误为 `0`。
- 店长页没有跨店选择器，且页面 DOM 不含未脱敏的其他门店工资；员工页没有外部工资记录链接。
- 桌面 Chromium 复跑 `00-health.spec.ts`、`01-role-routing.spec.ts`：`19/19` 通过，覆盖健康页、六角色 API 登录、默认工作台、无权限回退、督导正式角色归一和旧工作台链接回退。
- FIN-01/FIN-02/FIN-03/FIN-04 回归：财务接口 `2026-07` 仍为 2 条经营记录，营业收入 `21850.00`、成本 `8600.00`、费用 `9900.00`、净利 `3350.00`、净利率 `0.1533`；门店 A/B 分别为收入/净利 `9850/1950`、`12000/1400`；该月报销列表为 0。工资测试未向利润或报销重复累计。

## 5. 构建、自动化与修复

| 缺陷 | 最小修复 | 自动化与复测 |
| --- | --- | --- |
| IP-P1-014 | `SalaryWorkflowService` 增加金额、工时、公式、重复周期与并发冲突保护；`SalaryRepository` 同步 `net_pay`。 | `SalaryRecordRequestValidationTest`、`SalaryWorkflowServiceTest`、`SalaryServiceTest`、真实 QA 回归通过。 |
| IP-P1-015 | 审核通过状态更新时清空 `review_note`。 | H2 驳回重提通过回归和 QA MySQL 复测通过。 |
| IP-P1-016 | `AccessControlService` 与工资查询/工作流显式传递工资、门店、周期到拒绝审计。 | `AccessControlServiceTest`、`SalaryWorkflowServiceTest` 及四类真实越权请求通过。 |

- `mvn -q test`：通过。
- `mvn -q -DskipTests package`：通过。
- `npm run build`（含 `vue-tsc -b`）：通过。
- `npm run typecheck` 未单独执行：当前项目未定义该脚本；生产构建已包含正式类型检查。该命令差异为测试脚本配置问题，不影响构建结论。

## 6. QA 临时数据清理证明

清理前按精确 ID 登记 4 条临时工资、2 条工资明细、2 名临时员工、1 条工资规则、1 条员工工资档案、1 条考勤、测试 BOSS、关联待办、`operation_log` 和 E2E 会话。清理顺序先删除明细/待办/工资及依赖数据，再删除测试会话、审计和临时账号；未执行表级清空或影响 FIN-01 至 FIN-04 基线的数据删除。

清理后 QA MySQL 复核：上述工资、明细、员工、规则、档案、考勤、待办、测试 BOSS、测试会话及本轮审计残留均为 `0`；利润基线仍为 `2 / 21850.00 / 3350.00`。因此 QA 基线未变化。

下一门禁：按 G3 台账既定顺序进入 `FIN-06`。
