# FIN-04 报销录入与审批验收证据

执行批次：`E2E-20260720`
范围：隔离 QA MySQL、受控 E2E 门店与账号、桌面 Web（1280px）；不包含移动端，不读取或修改真实业务数据。

## 结论

`FIN-04 PASS`。所有业务、权限、并发、附件、审计、桌面页面和回归门禁均已通过；本轮受控临时单据及附件已精确清理，并复核不影响 FIN-01/FIN-02/FIN-03 QA 基线。

## 角色、范围与状态机

| 角色 | 已验证结论 |
| --- | --- |
| STORE_MANAGER A/B | 仅能为本门店创建、保存、提交、补资料和重提；伪造对方门店、跨品牌门店均被拒绝。 |
| FINANCE | 可在被授权两店范围内要求补资料、驳回、审批通过；通用财务待办完成入口不能绕过报销专用审批。 |
| BOSS | 按现有全局授权可查看并审批；门店 B 报销审批通过。 |
| SUPERVISOR | 已接管运营职责，但无报销阅读或审批权限；页面直达 `/admin/expenses` 跳转中文无权限页，接口为 403。 |
| WAREHOUSE、EMPLOYEE | 无报销审批权限；相关读写和越权附件访问均为 403。 |
| 匿名 | 报销列表、创建、上传和审批均为 401。 |

当前产品已实现且已验证的状态流转为：`草稿 -> 待审核 -> 已完成`，`待审核 -> 待补资料 -> 待审核 -> 已完成`，`待审核 -> 已拒绝 -> 待审核 -> 已完成`。已完成单据不可再编辑、审批或重复入账；当前没有“撤回”接口或旁路，已记录为现有产品规则而非擅自扩展。

## 接口、数据库、附件与并发证据

- 受控核心单据覆盖：主凭证上传、同一幂等键顺序/并发重试、键与载荷不一致 409、提交并发、审批并发、审批后不可变更；最终只保留一次状态迁移和一次审批结果。
- 补资料单据覆盖：要求补资料必须有中文说明、补充附件上传/预览/下载、驳回、重新提交、审批通过；补资料状态下不再出现可处理的财务待办。
- 门店 B 单据覆盖：店长 B 仅本店操作、店长 A 跨店拒绝、老板审批通过。
- 空月份、日期与月份不一致、负数、三位小数、空类别/说明、过长类别/说明、缺门店、品牌门店不匹配均返回中文 400 且无半条单据或附件关联。
- 非法类型、伪造文件签名和超限附件均被拒绝；跨门店附件读取/下载拒绝；有效附件的预览、下载、删除均校验登录、角色和门店范围。
- 5 个受控单据在清理前均位于 tenant `1`、月份 `2026-08`，其中 4 个为 `已完成`、1 个为 `待补资料`（仅用于验证“补资料待办关闭”）；主凭证共 5 条，补充附件 1 条。所有 ID、关联待办、操作日志及唯一文件路径均已在清理预检中精确登记并完成清理。
- 每次创建、提交、要求补资料、驳回、审批、附件上传/下载以及权限拒绝均核验 `operation_log` 的操作者、动作、目标单据、门店、月份、结果/原因和时间；拒绝日志由独立事务保留。

## 本轮修复与自动化回归

| 缺陷 | 根因与最小修复 | 回归 |
| --- | --- | --- |
| IP-P1-010 | 财务通用待办完成/上报老板路径可绕开报销专用状态机和审批审计。通用入口现在写入拒绝审计并返回专用工作流错误。 | `FinanceWorkbenchServiceTest`、`RoleTodoServiceTest` 与真实 QA 并发审批通过。 |
| IP-P1-011 | `待补资料` 单据仍可能保留活跃财务待办。活跃查询仅保留 `待审核/PENDING`。 | `TodoRepositoryScopeTest` 与真实 QA “请求补资料”场景通过。 |
| IP-P2-012 | 店长未传门店时未给出稳定中文字段错误。请求对象补充明确中文必填校验。 | `ExpenseClaimRequestValidationTest` 及真实 API 400 通过。 |
| IP-P1-013 | 品牌/门店不匹配拒绝审计缺少业务月份。授权拒绝入口显式传递月份。 | `BusinessScopeResolverTest` 与 QA `operation_log` 月份核验通过。 |

## 桌面页面与前序回归

- STORE_MANAGER 页面完成“新增报销 -> 上传主凭证 -> 保存并提交”；FINANCE 页面完成“通过”，页面刷新后显示 `已完成` 与财务说明。
- SUPERVISOR 直达报销页显示中文无权限页；未获得财务审批菜单或按钮。
- 报销页、利润概览、利润表、经营数据录入页在 `1280px` 下均为 `scrollWidth=clientWidth=1280`；控制台错误为 `0`。
- FIN-01/FIN-02 基线复核：`2026-07` 两条利润记录，营业收入 `21850.00`、净利 `3350.00`、门店数 `2`；数据库稳定指纹为 `babf952c66786bfce72fb2da626ae7b970fdfe967c0e79cad6bbede40c940914`。报销审批没有重复累计到利润数据。
- FIN-03 数据录入页面、历史记录和利润汇总正常显示，未写入新的经营数据。

## 构建与测试

- `mvn -q test`：通过。
- `mvn -q -DskipTests package`：通过。
- `npm run build`（包含 Vue 类型检查）：通过。
- `E2E_BASE_URL=http://127.0.0.1:8088/admin/ npx playwright test tests/e2e/33-expense-filtering-and-evidence.spec.ts --project=chromium`：4/4 通过，仅桌面 Chromium。
- 初次 Playwright 执行仅因本机缺少 Chromium 可执行文件而未启动；安装桌面 Chromium 后发现测试硬编码根路径，已改为相对路径，以同时兼容本地根路径和 QA `/admin/` 挂载路径，再次执行通过。该修复不改变业务功能。

## 清理复核

已按精确 ID、tenant `1` 清理本轮 5 个临时单据、5 条主凭证、1 条补充附件、6 条关联待办、12 条待办动作和 67 条操作日志；先提交数据库事务，后删除唯一经白名单校验的补充附件文件。最终逐表复核 `expense_claim`、`expense_supplement`、`expense_supplement_attachment`、`warehouse_attachment`、`business_todo`、`todo_action`、`todo_action_attachment`、`todo_escalation`、`operation_log` 均为 `0`，文件不存在；FIN-01/FIN-02 基线仍为 `2 / 21850.00 / 3350.00` 和相同稳定指纹。

下一门禁：`FIN-05 工资与本店工资核对`。
