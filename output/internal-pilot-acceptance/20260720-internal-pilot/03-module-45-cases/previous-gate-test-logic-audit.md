# WS-01 至 WS-05 测试逻辑复核

执行批次：`E2E-20260720`
范围：桌面 Web；不包含移动端。

## 结论

前五项已执行的正常、异常、范围、数据库和页面结论没有被本次复核推翻，但原测试逻辑存在若干缺口，已修正文档和补充角色矩阵。叫货数据库契约缺陷已修复，候选版本从 V72 变为 V74，并已完成新候选 G1 复核；G3 已恢复并通过 WS-06。

## 补充角色矩阵

下表为同一时点、同一 QA 数据库、六个正式角色对五个核心读取接口的实际 HTTP 结果；未登录访问五个接口均为 401。

| 角色 | WS-01 老板 | WS-02 财务 | WS-03 仓库 | WS-04 店长 | WS-05 巡检 |
| --- | ---: | ---: | ---: | ---: | ---: |
| BOSS | 200 | 200 | 200 | 403 | 200 |
| FINANCE | 403 | 200 | 403 | 403 | 403 |
| WAREHOUSE | 403 | 403 | 200 | 403 | 403 |
| STORE_MANAGER | 403 | 403 | 200 | 200 | 200 |
| SUPERVISOR | 403 | 403 | 403 | 403 | 200 |
| EMPLOYEE | 403 | 403 | 403 | 403 | 403 |

说明：`/api/warehouse/warehouses` 对店长返回其门店库存/供货所需的安全仓库范围；`/api/inspections` 对店长返回本店整改所需记录。这两项 200 不是越权，管理写接口仍由独立权限拒绝。`/api/store-manager/workbench` 必须具有明确店长门店上下文，因此 BOSS 不冒充店长，返回 403；BOSS 的全局门店能力在老板、门店治理及对应业务模块验证。

## 发现并纠正的问题

1. G3 台账曾把 WS-06 错写为“平台配置”；正式编号应为“督导运营工作台（原运营工作台）”。
2. 功能清单汇总把 7 个角色工作台误计为 6，虽总标题仍为 45 项，但分项求和会错误；已改为 7。
3. 验收矩阵残留“OPERATIONS 不负责日报损复核”及“督导日报损复核应 403”等迁移前表述；已统一为 SUPERVISOR 接管并允许授权门店复核。
4. 原台账以主角色和单个反向角色为主，未形成全角色矩阵；本次已补跑六角色矩阵并补充未登录 401。
5. 任何数据库异常不能直接算作目标缺陷复现。新增迁移测试明确检查根因列名，避免外键或夹具错误造成假阳性。
6. 财务拥有 `inventory.read`，读取盘存列表返回 200 是合法行为，不能作为督导运营越权反例；WS-06 已改用财务访问考试中心 403 验证反向权限。

## 候选变更影响

叫货修复新增 MySQL V73/V74、H2 V72/V73，并修改 Repository 写入 SQL。V74 空库、升级迁移、全量测试和最新 JAR 哈希均已重建；WS-06 的中文错误与审计修复也已重新执行受影响的 G1、API、数据库和页面验证。
