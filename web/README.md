# 门店利润系统新工作台

阶段 1 新前端骨架，采用 Vue 3 + TypeScript + Vite。

## 开发

```powershell
npm install
npm run dev
```

默认访问：

```text
http://127.0.0.1:5173
```

开发服务器会把 `/api` 代理到 `http://localhost:8080`。

## 构建

```powershell
npm run build
```

当前阶段只提供新工作台骨架，不替代旧 `index.html`。
