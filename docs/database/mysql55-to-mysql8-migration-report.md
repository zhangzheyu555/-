# MySQL 5.5 到 MySQL 8 逻辑迁移报告

> 状态：**BLOCKED**  
> 取证时间：2026-07-12（Asia/Shanghai）  
> 迁移方式：仅允许逻辑导出/导入；本次未复制数据目录、未修改源库、未执行切换。

## 1. 结论

本次在制作 SQL 备份之前触发三个硬门禁，因此按迁移规则立即停止：

1. 只读检查期间，原本正常的 MySQL 5.5 源服务于 2026-07-12 09:13:28 被系统记录为意外终止（SCM 事件 7034，服务退出码 1067）。Codex 没有停止或启动该服务；按“源端关键检查失败立即停止”的规则，没有尝试自动恢复。
2. `127.0.0.1:3306` 停止前的运行数据库报告 Flyway 已到 V37，但当前工作树和当前 Jar 的 MySQL 迁移只到 V34，MySQL V37 最终原件及权威 checksum 缺失。
3. 3307 检查期间经历了“服务停止但有未知控制台实例”“无监听”“服务实例启动”三次外部变化。最新快照已确认监听进程是 `MySQL80Test` 的子进程，但当前用户仍无权读取其数据目录、子 ACL 和错误日志，且 `tmpdir` 未显式配置，服务启动根因和目录权限证据仍不完整。

在这三个门禁解除前，不允许制作目标库、导入、运行 Flyway、启动 18082 或切换 18081。

## 2. 服务与端口快照

| 项目 | 只读检查结果 |
|---|---|
| MySQL 5.5 源端 | 初始 `127.0.0.1:3306` TCP 握手为 `5.5.62`；09:13:28 后服务意外停止，当前无监听 |
| MySQL 8 目标端 | 最新 `127.0.0.1:3307` TCP 握手为 `8.0.46` |
| `MySQL80Test` | 最新状态 `Running / Manual`；由外部状态变化启动，并非本迁移脚本启动 |
| 3307 实际监听 | 最新快照为服务父进程 PID `8168` 的子进程 PID `520`，仅监听 `127.0.0.1`；PID 仅用于该时点取证 |
| MySQL 8 配置 | `port=3307`、`bind-address=127.0.0.1`，配置校验退出码 `0` |
| MySQL 8 数据目录与错误日志 | 当前用户读取被拒绝，初始化状态、子目录 ACL 和真实错误摘要尚未证实 |
| ACL 修改 | **未执行** |
| 旧后端 | 进程仍监听 `http://127.0.0.1:18081`，Spring Boot 3.5.16，`STAGING`；源库停止后健康检查不可用 |
| 旧后端数据库连接 | 源库停止前已确认连接 `127.0.0.1:3306` |
| 前端 | `http://127.0.0.1:5173`，当前代理到 18081 |

MySQL 8 服务配置路径：`C:\ProgramData\MySQL\MySQL Server 8.0\my.ini`。配置的数据目录为 `C:\ProgramData\MySQL\MySQL Server 8.0\Data`；错误日志配置为该数据目录下的相对日志文件。报告只记录路径和可访问状态，没有复制日志内容。

### ACL 后续计划（尚未授权、尚未执行）

只有确认 3307 未知进程来源并能读取实际数据目录后，才重新计算权限差异。如果错误根因确认为权限，拟仅对以下主体和实际目录授权：

- 主体：`NT AUTHORITY\NetworkService`（`MySQL80Test` 实际服务账号）；
- 目录：经配置确认的 `datadir`、`tmpdir` 和日志目录；
- 权限：运行所需的读取、写入和修改；
- 明确禁止：对 `Everyone`、`Users` 或磁盘根目录授予完全控制。

执行任何 ACL 修改前必须再次输出精确目录、账号和权限差异并取得确认。

## 3. Flyway 发布链审计

| 证据层 | MySQL 迁移最高版本 | V35 | V36 | V37 |
|---|---:|---|---|---|
| Git HEAD | V29 | 缺失 | 缺失 | 缺失 |
| Git 暂存区 | V36 | 存在 | 存在 | 缺失 |
| 当前工作树 | V34 | 已暂存后又被删除（`AD`） | 已暂存后又被删除（`AD`） | 缺失 |
| 当前 Jar | V34 | 缺失 | 缺失 | 缺失 |
| 3306 运行数据库 | V37 | 已应用状态待安全查询 | 已应用状态待安全查询 | 已应用状态待安全查询 |
| H2 迁移目录 | V37 | 存在 | 存在 | 存在，但不能代替 MySQL 版本 |

当前磁盘 Jar（JVM `user.dir` 与 `java.class.path` 均指向该路径；运行进程本身不提供已加载字节的独立哈希）：

- SHA-256：`BBFB26402D103C33905552AC3B0CDEA45A821381E48E5E1203F1EAFA08BB006F`
- 包内 MySQL Flyway 迁移：V1–V34
- 18081 的启动时间与该 Jar 写入时间一致，但数据库健康信息为 V37；在没有运行中字节哈希的情况下，至少可以确认候选源码、磁盘 Jar 与数据库历史不自洽。

V35、V36 可由冻结发布候选和 Git 暂存对象交叉证明；V37 不能：

- 找到的 MySQL V37 不可达 Git blob 是一个已知失败的中间稿，仍包含 MySQL 8 不支持的两处 `ADD COLUMN IF NOT EXISTS`，且缺最终约束，禁止恢复。
- 可以依据历史修复记录推导候选内容和 checksum，但推导值不等于权威原件，不能用于恢复已执行迁移。
- 只有从可信发布产物/备份找回最终 V37 原件，并与 3306 `flyway_schema_history` 的 script、checksum、success 精确匹配，才可解除门禁。

此外，当前 `application.yml` 启用了 `baseline-on-migrate`。后续迁移验收启动必须显式设置 `SPRING_FLYWAY_BASELINE_ON_MIGRATE=false`，并拒绝“非空库但缺少 `flyway_schema_history`”的情况，防止自动基线掩盖历史缺失。

## 4. 数据库、备份与导入结果

本次只读预检证据保存在仓库外受限目录：

- 路径：`C:\Users\34706\AppData\Local\AI-Profit-OS\mysql55-to-mysql8\preflight-codex-20260712-v5\mysql8-preflight.json`
- SHA-256：`C6C8F7A3F36FDF5CC4573410B472C731BBBE162801BB3335801CA8CFFBE3A8C9`
- 状态：`BLOCKED`

| 项目 | 结果 |
|---|---|
| 源业务库 | `store_profit`（由当前后端脱敏运行证据确认） |
| 目标业务库 | 未创建 |
| 3307 现有库备份 | 未执行；硬门禁发生在凭据输入和数据库访问之前 |
| 3306 原始 SQL 备份 | 未执行 |
| SQL SHA-256 | 不适用 |
| 逻辑导入 | 未执行 |
| Flyway validate/migrate | 未执行 |
| 表数、逐表行数、金额、库存、月份对比 | 未执行 |
| 18082 新后端 | 未启动 |
| 浏览器与角色验收 | 未执行 |
| 18081 正式切换 | 未执行 |

这不是“迁移失败后留下半成品库”：迁移在任何数据库写入前停止。3306 的意外退出是审计期间发生的外部状态变化，不是迁移脚本造成；18081 进程和 5173 仍在，但数据库功能需等待源服务由其责任方安全恢复。

## 5. 已生成的安全脚本

- `scripts/mysql8-migration-preflight.ps1`
  - 只读核对服务、端口、无认证版本握手、配置、目录可访问性、错误日志摘要和进程归属；
  - 审计输出强制写入仓库外受限目录；
  - 不修改服务、ACL、配置或数据库。
- `scripts/mysql8-logical-migration.ps1`
  - 暴露 `Preflight`、`DryRun`、`Validate`、`Cutover` 和 `Rollback` 模式名；
  - `DryRun` 先过服务和 Flyway 门禁，再备份 3307 非系统库；
  - 在任何 `CREATE DATABASE`/导入前检查零日期、移除的 SQL_MODE、缺失 DEFINER、MySQL 8 保留字，并用认证连接核对 `@@port/@@datadir/@@tmpdir`；
  - 使用 MySQL 5.5 自带 `mysqldump --result-file` 制作不可变原始 SQL；
  - 只向全新的目标库导入，失败时只回收本次新建的目标库，不触碰已有库；
  - 当前 `Cutover` 与 `Rollback` **故障关闭**：在权威 V37、Jar/Flyway 双启动、最小权限账号、浏览器审批绑定和可审计进程状态机补齐前会立即拒绝执行；
  - 当前 `DryRun` 仅是逻辑备份/导入骨架，尚未实现最新 Jar 的 Flyway validate/migrate、两次启动和 18082 验收，不能作为正式切换工具。
- `scripts/mysql8-validate-migration.ps1`
  - 强制 TCP 比较版本、对象数、所有业务表真实 `COUNT(*)`、关键金额/工资/库存聚合、月份、Flyway 历史、密码哈希指纹和乱码增量；
  - 对列语义、索引、外键以及视图/触发器/过程/事件定义摘要做对比；跨版本已知等价的整数显示宽度和 `DEFAULT_GENERATED` 会先规范化；
  - 精确数量和聚合只进入仓库外受限 JSON，不写入仓库或聊天。

三个脚本均通过 PowerShell AST 语法解析，`Preflight` 已实际运行并按预期返回 `BLOCKED`；`DryRun/Validate/Cutover/Rollback` 未在数据库上执行。凭据只通过隐藏输入进入 ACL 仅当前用户可读的临时 `--defaults-extra-file`，该参数始终位于客户端第一个参数，且在 `finally` 中覆盖并删除。脚本不使用 PowerShell `>` 保存 SQL。

## 6. 回滚状态

本次没有导入或切换，所以无需执行数据回滚：

1. 旧后端 18081 的配置仍指向 3306，但源服务当前意外停止；
2. Codex 未停止、启动、修改或升级 3306；
3. 3307 未创建目标业务库；
4. 没有 MySQL 8 数据需要反向导入 MySQL 5.5；
5. 未删除任何现有数据、配置或进程。

## 7. 解除阻塞的顺序

1. 由源实例责任方先核对 MySQL 5.5 错误日志与意外退出原因，确认数据目录未被其他进程占用后安全恢复 3306；本迁移任务不自行猜测或初始化源实例。
2. 确认 3307 控制台 `mysqld` 的启动来源、实际 `defaults-file` 和 `datadir`；在确认前不启停 `MySQL80Test`。
3. 通过安全凭据只读查询 3306 的 V35–V37 `script/checksum/success`，不输出密码或业务明细。
4. 从可信发布产物或备份找回 MySQL V37 最终原件；禁止依据 H2 文件或中间稿重写同版本迁移。
5. 恢复可核验的 V35–V37 后重新构建 Jar，确认 Jar、源码和数据库历史完全一致。
6. 重新运行 `Preflight`；通过后才允许 `DryRun` 制作逻辑备份、备份 3307、导入新目标库并执行一致性验收。
7. 演练、18082、浏览器、角色和自动测试全部 PASS 后，另行批准 `Cutover`。

## 8. 未解决风险

- 3307 最新监听已由 `MySQL80Test` 控制，但数据目录 ACL、错误日志和服务账号 `tmpdir` 权限仍不可证实；
- 3306 源服务在审计期间意外停止，尚未完成责任方恢复和根因确认；
- MySQL V37 最终迁移原件与权威 checksum 缺失；
- 当前 Jar 与运行数据库 Flyway 状态不一致；
- 尚未取得源/目标数据库安全凭据，因此未执行对象、MyISAM、零日期、DEFINER、字符集和逐表基线检查；
- 尚无 SQL 备份及其 SHA-256，不能进入数据导入阶段；
- 尚未执行后端、Vue、E2E 和浏览器迁移验收。
- `Cutover/Rollback` 当前故障关闭；逻辑脚本还不是生产切换状态机。
