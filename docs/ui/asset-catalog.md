# 视觉资产清单

| 资产 | 来源 | 使用方式 | 说明 |
| --- | --- | --- | --- |
| 最终设计稿 | `outputs/product-design/ai-profit-os-ui-final.png` | 视觉对照，不进入构建产物 | 作为桌面端布局、色彩、间距和信息层级基准 |
| 系统标识 | Vue3 CSS 设计令牌 | 侧栏和登录页的橙色方形标识 | 不使用外部品牌图，不增加网络依赖 |
| 业务图标 | `lucide-vue-next` | 菜单、KPI、操作按钮和状态 | 延续项目现有图标库，避免自绘 SVG |
| 现场凭证 | `/api/todos/{todoId}/attachments/{attachmentId}` | 老板待复核列表最多展示已有图片缩略图 | 通过受保护 API 获取 Blob，不写死图片、不暴露附件地址 |
| 经营趋势 | Vue Canvas | 老板工作台近七月营业额趋势 | 使用真实利润数据绘制，不保存为静态图片 |

## 资产原则

- 不使用模拟营业数据、占位业务照片或旧 HTML 图片数据。
- 无现场凭证时不显示空图片框。
- 附件预览失败只记录控制台信息，不影响待办处理。
- 图标仅表达操作和状态，不作为装饰背景。
